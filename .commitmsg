R51-R60: Rename atomicity, Overwrite/SetLength semantics, ReadDirectory, validation

- R51: Rename replace with open handles (NTFS PendingDelete semantics)
- R52: Overwrite callback semantics (truncate + keep SD/CreationTime)
- R53-R55: SetLength expansion (sparse reserve) and truncation (page release + partial zero)
- R56-R58: ReadDirectory implementation (snapshot + sort + marker continuation)
- R59: Phase 1 acceptance checklist alignment verified
- R60: Cross-plan reference integrity check (all 7 child plans linked)
