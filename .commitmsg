四巨头审查 R1-R10：伪代码严谨性修复 — CAS竞态、异常安全、原子性

【Google 总监】
- 修复 TryReserve CAS 竞态：增加后置校验，超额时回退预留
- Write 伪代码：禁止跨页 CopyBlock，强制逐页 memcpy
- TryRent 完整伪代码：容量快速检查 + OOM 捕获 + 计数器维护

【Microsoft 总监】
- 修复 Rename rollback 原子性：先检查存在性再 TryRemove，
  避免 rollback TryAdd 失败导致节点丢失
- 完善 Cleanup/Close 锁序注释

【Amazon 总监】
- PagePool.Return 增加页面有效性校验（page==0 抛异常）
- Return 不检查 _disposed：Dispose 后延迟 Return 仍归还，
  Dispose 统一 Free 全部页面
- PagePool.Dispose 完整伪代码：泄漏检测 + 逐页 Free + 日志

【Oracle 总监】
- BatchLease.Commit 增加边界检查 + double-commit 防护 + Dispose 后防护
- PageLease 增加 Debug.Assert + double-commit 防护 + Dispose 后 page=0
- Read 完整伪代码：读锁全程覆盖 + 稀疏零填充 + FileSize clamp
- Write 完整伪代码：三阶段含 TOCTOU 重检 + FileSize/AllocationSize 事务提交
- TryRentFromReservation 完整伪代码：CAS 扣减预留 + OOM 回退
