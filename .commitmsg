四巨头审查 R21-R30：性能工程 — 零分配热路径、ArrayPool、PipeFraming优化

【Google 总监】
- Write Phase 1 扫描：stackalloc/ArrayPool 替代 new List<int> 减少 GC
- Write 方法末尾归还 ArrayPool 租借数组
- PipeFraming.WriteMessageAsync：消息大小上限检查

【Microsoft 总监】
- PagePool 增加 _freeCount 计数器字段声明（替代 O(n) ConcurrentStack.Count）
- EnsurePageTableCapacity 在读锁内预检

【Amazon 总监】
- PipeFraming 修复 header.ToArray() 堆分配
- WriteMessageAsync 增加 FlushAsync 确保对端立即收到
- ReadMessageAsync 增加消息大小上限校验

【Oracle 总监】
- Read 热路径保持读锁全程覆盖，数据安全优先于性能
- Write Phase 3 中 missingIndices 迭代从 Span 直接读取
